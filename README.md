# Digit-Recognition-System
This project implements a handwritten digit recognition system using an Artificial Neural Network (ANN) and the MNIST dataset. The model is trained to recognize digits from 0 to 9 with high accuracy. The project includes data preprocessing, model training, evaluation, and prediction using Python and TensorFlow/Keras.
